// Code generated by sdk-factory (scripts/generate.sh); DO NOT EDIT.
//
// The service fields below are derived from the generated client in
// src/_generated, so a service added by a spec change appears here
// automatically. Base-URL precedence comes from manifest/sdk.yaml.

import axios, { type AxiosInstance } from 'axios';
import {
  DEFAULT_TIMEOUT_MS,
  DEFAULT_USER_AGENT,
  resolveBaseURL,
  validate,
  type ClientOptions,
} from './config';
import { installAuthInterceptor } from './auth';
import { installErrorInterceptor } from './errors';
import { Configuration } from './_generated/configuration';
import { operationServerMap } from './_generated/base';
import {
  AccountCustomObjectApi,
  AccountSecurityApi,
  AccountSessionApi,
  AccountsApi,
  BigCommerceSSOApi,
  CaptchaConfigurationApi,
  ConsentApi,
  CrossDeviceSSOApi,
  CustomFieldsApi,
  CustomObjectApi,
  CustomObjectsApi,
  DomainAccessRestrictionsApi,
  EmailTemplatesApi,
  IPAccessRestrictionsApi,
  IdentityApi,
  InsightsApi,
  JWTApi,
  JWTCustomProvidersApi,
  JWTIntegrationsApi,
  LoginApi,
  MultipurposeTokensApi,
  OAuthApi,
  OAuthClientsApi,
  OAuthCustomProvidersApi,
  OAuthIntegrationsApi,
  OAuthM2MApi,
  OIDCApi,
  OrganizationApi,
  OrganizationConnectionGroupRolesApi,
  OrganizationConnectionsApi,
  OrganizationDomainsApi,
  OrganizationInvitationsApi,
  OrganizationUserRolesApi,
  PasskeyConfigurationApi,
  PasswordApi,
  PasswordPolicyApi,
  PerfectMindSSOApi,
  PermissionsApi,
  PushNotificationConfigurationApi,
  RegistrationApi,
  RolesApi,
  RolesManagementApi,
  SAMLApi,
  SAMLCustomProvidersApi,
  SAMLIntegrationsApi,
  SMSTemplatesApi,
  SOTTApi,
  SecondFactorConfigurationApi,
  SecurityApi,
  SecurityQuestionsApi,
  SessionApi,
  ShopifySSOApi,
  SocialProvidersApi,
  UserApi,
  UserMigrationApi,
  WebhooksApi,
  WorkflowsApi,
} from './_generated/api';

/**
 * Entry point for all LoginRadius API calls. Construct once per tenant /
 * credential set and reuse — every service field on the resulting client
 * shares the underlying axios instance, interceptors, and configuration.
 *
 * @example
 * ```ts
 * import { LoginRadius } from 'loginradius-sdk';
 *
 * const client = new LoginRadius({ apiKey: process.env.LR_API_KEY });
 * const res = await client.login.checkUserNameAvailability({ username: 'alice' });
 * ```
 */
export class LoginRadius {
  // One readonly field per OpenAPI tag — 57 services. Names use
  // lowerCamelCase for natural JS call sites (client.login.loginByEmail rather
  // than client.LoginApi.loginByEmail).
  readonly accountCustomObject: AccountCustomObjectApi;
  readonly accountSecurity: AccountSecurityApi;
  readonly accountSession: AccountSessionApi;
  readonly accounts: AccountsApi;
  readonly bigCommerceSSO: BigCommerceSSOApi;
  readonly captchaConfiguration: CaptchaConfigurationApi;
  readonly consent: ConsentApi;
  readonly crossDeviceSSO: CrossDeviceSSOApi;
  readonly customFields: CustomFieldsApi;
  readonly customObject: CustomObjectApi;
  readonly customObjects: CustomObjectsApi;
  readonly domainAccessRestrictions: DomainAccessRestrictionsApi;
  readonly emailTemplates: EmailTemplatesApi;
  readonly ipAccessRestrictions: IPAccessRestrictionsApi;
  readonly identity: IdentityApi;
  readonly insights: InsightsApi;
  readonly jwt: JWTApi;
  readonly jwtCustomProviders: JWTCustomProvidersApi;
  readonly jwtIntegrations: JWTIntegrationsApi;
  readonly login: LoginApi;
  readonly multipurposeTokens: MultipurposeTokensApi;
  readonly oauth: OAuthApi;
  readonly oauthClients: OAuthClientsApi;
  readonly oauthCustomProviders: OAuthCustomProvidersApi;
  readonly oauthIntegrations: OAuthIntegrationsApi;
  readonly oauthM2M: OAuthM2MApi;
  readonly oidc: OIDCApi;
  readonly organization: OrganizationApi;
  readonly organizationConnectionGroupRoles: OrganizationConnectionGroupRolesApi;
  readonly organizationConnections: OrganizationConnectionsApi;
  readonly organizationDomains: OrganizationDomainsApi;
  readonly organizationInvitations: OrganizationInvitationsApi;
  readonly organizationUserRoles: OrganizationUserRolesApi;
  readonly passkeyConfiguration: PasskeyConfigurationApi;
  readonly password: PasswordApi;
  readonly passwordPolicy: PasswordPolicyApi;
  readonly perfectMindSSO: PerfectMindSSOApi;
  readonly permissions: PermissionsApi;
  readonly pushNotificationConfiguration: PushNotificationConfigurationApi;
  readonly registration: RegistrationApi;
  readonly roles: RolesApi;
  readonly rolesManagement: RolesManagementApi;
  readonly saml: SAMLApi;
  readonly samlCustomProviders: SAMLCustomProvidersApi;
  readonly samlIntegrations: SAMLIntegrationsApi;
  readonly smsTemplates: SMSTemplatesApi;
  readonly sott: SOTTApi;
  readonly secondFactorConfiguration: SecondFactorConfigurationApi;
  readonly security: SecurityApi;
  readonly securityQuestions: SecurityQuestionsApi;
  readonly session: SessionApi;
  readonly shopifySSO: ShopifySSOApi;
  readonly socialProviders: SocialProvidersApi;
  readonly user: UserApi;
  readonly userMigration: UserMigrationApi;
  readonly webhooks: WebhooksApi;
  readonly workflows: WorkflowsApi;

  private readonly httpClient: AxiosInstance;

  constructor(options: ClientOptions) {
    validate(options);

    // Normalise options once so the interceptors see resolved defaults.
    const resolved: ClientOptions = {
      ...options,
      userAgent: options.userAgent ?? DEFAULT_USER_AGENT,
    };

    // Reuse caller's axios instance when supplied — preserves their proxy,
    // httpsAgent, transformRequest, etc. We always add our interceptors on
    // top so credential injection and error mapping are still in effect.
    const http =
      options.httpClient ?? axios.create({ timeout: options.timeout ?? DEFAULT_TIMEOUT_MS });
    if (options.httpClient && options.timeout !== undefined) {
      // The caller supplied both, so they asked for it. Without an explicit
      // timeout we leave their instance alone rather than imposing our default
      // on something they configured themselves.
      http.defaults.timeout = options.timeout;
    }
    installAuthInterceptor(http, resolved);
    installErrorInterceptor(http);
    this.httpClient = http;

    const basePath = resolveBaseURL(options);
    // `basePath` is deliberately NOT put on Configuration.
    //
    // The generated request builder resolves its URL as
    //   (configuration?.basePath ?? basePath) + path
    // so a basePath on Configuration outranks the per-operation server the
    // spec pins — meaning operations that genuinely live on cloud-api or
    // migration.loginradius.com would be sent to the tenant host and 404.
    //
    // Passing it as each service's `basePath` argument instead keeps normal
    // operations on the configured host while letting pinned operations use
    // their own, which is what applyOperationServers below reconciles.
    const apiConfig = new Configuration();
    // Reconcile the per-operation server lists the spec pins for
    // 42 operations. Without this the generated layer
    // silently ignores `baseURL` for them, so a caller pointing the SDK at a
    // proxy or staging host would still see part of their traffic go to
    // production.
    applyOperationServers(options, options.serverIndex);

    this.accountCustomObject = new AccountCustomObjectApi(apiConfig, basePath, http);
    this.accountSecurity = new AccountSecurityApi(apiConfig, basePath, http);
    this.accountSession = new AccountSessionApi(apiConfig, basePath, http);
    this.accounts = new AccountsApi(apiConfig, basePath, http);
    this.bigCommerceSSO = new BigCommerceSSOApi(apiConfig, basePath, http);
    this.captchaConfiguration = new CaptchaConfigurationApi(apiConfig, basePath, http);
    this.consent = new ConsentApi(apiConfig, basePath, http);
    this.crossDeviceSSO = new CrossDeviceSSOApi(apiConfig, basePath, http);
    this.customFields = new CustomFieldsApi(apiConfig, basePath, http);
    this.customObject = new CustomObjectApi(apiConfig, basePath, http);
    this.customObjects = new CustomObjectsApi(apiConfig, basePath, http);
    this.domainAccessRestrictions = new DomainAccessRestrictionsApi(apiConfig, basePath, http);
    this.emailTemplates = new EmailTemplatesApi(apiConfig, basePath, http);
    this.ipAccessRestrictions = new IPAccessRestrictionsApi(apiConfig, basePath, http);
    this.identity = new IdentityApi(apiConfig, basePath, http);
    this.insights = new InsightsApi(apiConfig, basePath, http);
    this.jwt = new JWTApi(apiConfig, basePath, http);
    this.jwtCustomProviders = new JWTCustomProvidersApi(apiConfig, basePath, http);
    this.jwtIntegrations = new JWTIntegrationsApi(apiConfig, basePath, http);
    this.login = new LoginApi(apiConfig, basePath, http);
    this.multipurposeTokens = new MultipurposeTokensApi(apiConfig, basePath, http);
    this.oauth = new OAuthApi(apiConfig, basePath, http);
    this.oauthClients = new OAuthClientsApi(apiConfig, basePath, http);
    this.oauthCustomProviders = new OAuthCustomProvidersApi(apiConfig, basePath, http);
    this.oauthIntegrations = new OAuthIntegrationsApi(apiConfig, basePath, http);
    this.oauthM2M = new OAuthM2MApi(apiConfig, basePath, http);
    this.oidc = new OIDCApi(apiConfig, basePath, http);
    this.organization = new OrganizationApi(apiConfig, basePath, http);
    this.organizationConnectionGroupRoles = new OrganizationConnectionGroupRolesApi(
      apiConfig,
      basePath,
      http,
    );
    this.organizationConnections = new OrganizationConnectionsApi(apiConfig, basePath, http);
    this.organizationDomains = new OrganizationDomainsApi(apiConfig, basePath, http);
    this.organizationInvitations = new OrganizationInvitationsApi(apiConfig, basePath, http);
    this.organizationUserRoles = new OrganizationUserRolesApi(apiConfig, basePath, http);
    this.passkeyConfiguration = new PasskeyConfigurationApi(apiConfig, basePath, http);
    this.password = new PasswordApi(apiConfig, basePath, http);
    this.passwordPolicy = new PasswordPolicyApi(apiConfig, basePath, http);
    this.perfectMindSSO = new PerfectMindSSOApi(apiConfig, basePath, http);
    this.permissions = new PermissionsApi(apiConfig, basePath, http);
    this.pushNotificationConfiguration = new PushNotificationConfigurationApi(
      apiConfig,
      basePath,
      http,
    );
    this.registration = new RegistrationApi(apiConfig, basePath, http);
    this.roles = new RolesApi(apiConfig, basePath, http);
    this.rolesManagement = new RolesManagementApi(apiConfig, basePath, http);
    this.saml = new SAMLApi(apiConfig, basePath, http);
    this.samlCustomProviders = new SAMLCustomProvidersApi(apiConfig, basePath, http);
    this.samlIntegrations = new SAMLIntegrationsApi(apiConfig, basePath, http);
    this.smsTemplates = new SMSTemplatesApi(apiConfig, basePath, http);
    this.sott = new SOTTApi(apiConfig, basePath, http);
    this.secondFactorConfiguration = new SecondFactorConfigurationApi(apiConfig, basePath, http);
    this.security = new SecurityApi(apiConfig, basePath, http);
    this.securityQuestions = new SecurityQuestionsApi(apiConfig, basePath, http);
    this.session = new SessionApi(apiConfig, basePath, http);
    this.shopifySSO = new ShopifySSOApi(apiConfig, basePath, http);
    this.socialProviders = new SocialProvidersApi(apiConfig, basePath, http);
    this.user = new UserApi(apiConfig, basePath, http);
    this.userMigration = new UserMigrationApi(apiConfig, basePath, http);
    this.webhooks = new WebhooksApi(apiConfig, basePath, http);
    this.workflows = new WorkflowsApi(apiConfig, basePath, http);
  }

  /**
   * Returns the underlying axios instance. Exposed for tests that need to
   * swap or inspect the transport; production code should configure via
   * `httpClient` at construction time.
   */
  getHttpClient(): AxiosInstance {
    return this.httpClient;
  }
}

// The generator emits `operationServerMap` as module-level mutable state, so a
// naive rewrite is destructive: the first client to substitute `{domain}` would
// bake its tenant in permanently and every later client would inherit it. This
// snapshot preserves the pristine spec values so each client re-derives from
// them, letting one process serve several tenants correctly.
const PRISTINE_OPERATION_SERVERS = JSON.parse(
  JSON.stringify(operationServerMap),
) as typeof operationServerMap;

/**
 * Rewrites `operationServerMap` so the pinned operations honour this client's
 * server selection, always starting from the pristine spec values.
 *
 * Rules:
 *   - An explicit `baseURL` means "send everything here", so it replaces every
 *     pinned list.
 *   - Otherwise the pinned hosts stand, but their template variables are
 *     filled from `domain` / `customDomain` instead of the spec's placeholder
 *     tenant.
 *   - `serverIndex` promotes one entry of each list to position 0.
 *
 * Note this still writes shared module state, so constructing two clients with
 * different server options and interleaving their calls will use whichever was
 * built last. Build clients per configuration and use them in turn, or pass
 * `baseURL`.
 */
function applyOperationServers(options: ClientOptions, serverIndex?: number): void {
  // Restore the spec values before applying this client's configuration.
  for (const [key, entries] of Object.entries(PRISTINE_OPERATION_SERVERS)) {
    operationServerMap[key] = JSON.parse(JSON.stringify(entries));
  }

  if (options.baseURL) {
    const url = options.baseURL.replace(/\/+$/, '');
    for (const key of Object.keys(operationServerMap)) {
      operationServerMap[key] = [{ url, description: 'Configured baseURL' }];
    }
    return;
  }

  const vars: Record<string, string | undefined> = {
    TenantName: options.domain,
    CustomDomain: options.customDomain,
    domain: options.domain,
  };
  // The template variables must be substituted into the URL STRING: unlike the
  // Go generator, typescript-axios reads `entry.url` verbatim and never
  // expands `{placeholders}`, so writing `default_value` alone would leave a
  // literal "https://{domain}.hub.loginradius.com" on the wire.
  for (const entries of Object.values(operationServerMap)) {
    for (const entry of entries) {
      for (const [name, value] of Object.entries(entry.variables ?? {})) {
        const replacement = vars[name] ?? value.default_value;
        if (replacement) {
          entry.url = entry.url.split(`{${name}}`).join(replacement);
          value.default_value = replacement;
        }
      }
    }
  }

  if (serverIndex !== undefined && serverIndex > 0) {
    for (const [key, entries] of Object.entries(operationServerMap)) {
      if (serverIndex >= entries.length) continue;
      operationServerMap[key] = [
        entries[serverIndex],
        ...entries.filter((_, i) => i !== serverIndex),
      ];
    }
  }
}
