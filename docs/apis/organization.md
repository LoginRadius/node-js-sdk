<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Organization

8 operation(s). Part of the [API index](../API.md).

Call these as `client.<service>.<method>({ ... })`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `createOrganization`

**POST** `/v2/manage/organizations`

Create Organization

Creates a new Organization in the Tenant.

### Example

```typescript
async function callCreateOrganization(client: LoginRadius) {
  const object = {  }; //Required

  try {
    const res = await client.<service>.createOrganization({ object });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Request body

`object` as `application/json`.

### Returns

`OrganizationsResponse`

---

## `createOrgTenantRole`

**POST** `/v2/manage/organizations/{orgId}/roles`

Create Role in Organization

Creates a Role within an Organization.

### Example

```typescript
async function callCreateOrgTenantRole(client: LoginRadius) {
  const orgId: string = '<orgId>'; //Required
  const rolePostRequest = {  }; //Required

  try {
    const res = await client.<service>.createOrgTenantRole({ orgId, rolePostRequest });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`RolePostRequest` as `application/json`.

### Returns

`Role`

---

## `deleteOrganization`

**DELETE** `/v2/manage/organizations/{orgId}`

Delete Organization

Deletes an Organization by its ID.

### Example

```typescript
async function callDeleteOrganization(client: LoginRadius) {
  const orgId: string = '<orgId>'; //Required

  try {
    const res = await client.<service>.deleteOrganization({ orgId });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`DeleteResponse`

---

## `getAllOrganizations`

**GET** `/v2/manage/organizations`

List Organizations

Retrieves a list of all Organizations in the Tenant.

### Example

```typescript
async function callGetAllOrganizations(client: LoginRadius) {
  try {
    const res = await client.<service>.getAllOrganizations();
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Returns

`object`

---

## `getOrganization`

**GET** `/v2/manage/organizations/{orgId}`

Retrieve Organization details

Retrieves details of a specific Organization by its ID.

### Example

```typescript
async function callGetOrganization(client: LoginRadius) {
  const orgId: string = '<orgId>'; //Required

  try {
    const res = await client.<service>.getOrganization({ orgId });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`OrganizationsResponse`

---

## `getOrgContextByOrgId`

**GET** `/v2/manage/organizations/{orgId}/orgcontext`

Retrieve Organization context

Retrieves User Roles for all Organizations by OrgID.

### Example

```typescript
async function callGetOrgContextByOrgId(client: LoginRadius) {
  const orgId: string = '<orgId>'; //Required

  try {
    const res = await client.<service>.getOrgContextByOrgId({ orgId });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Unique identifier of the Organization. |

### Returns

`object`

---

## `getOrgRolesByOrgId`

**GET** `/v2/manage/organizations/{orgId}/roles`

List Organization Roles

Lists all Roles defined within an Organization.

### Example

```typescript
async function callGetOrgRolesByOrgId(client: LoginRadius) {
  const orgId: string = '<orgId>'; //Required

  try {
    const res = await client.<service>.getOrgRolesByOrgId({ orgId });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Returns

`object`

---

## `updateOrganization`

**PUT** `/v2/manage/organizations/{orgId}`

Update Organization

Updates an Organization by its ID. Supports updating org fields, policies, and status in a single request.

### Example

```typescript
async function callUpdateOrganization(client: LoginRadius) {
  const orgId: string = '<orgId>'; //Required
  const organizationUpdateRequest = {  }; //Required

  try {
    const res = await client.<service>.updateOrganization({ orgId, organizationUpdateRequest });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `orgId` | path | string | yes | Organization ID |

### Request body

`OrganizationUpdateRequest` as `application/json`.

### Returns

`OrganizationsResponse`

