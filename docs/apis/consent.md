<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Consent

7 operation(s). Part of the [API index](../API.md).

Call these as `client.<service>.<method>({ ... })`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `addConsentForm`

**POST** `/consent/forms`

Add Consent Form

Adds a new Consent Form for the Tenant.

### Example

```typescript
async function callAddConsentForm(client: LoginRadius) {
  const consentFormModel = {  }; //Required

  try {
    const res = await client.<service>.addConsentForm({ consentFormModel });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Request body

`ConsentFormModel` as `application/json`.

### Returns

`ConsentForm`

---

## `createConsentOption`

**POST** `/consent/options`

Create Consent Option

Creates a new consent option for a specific Tenant.

### Example

```typescript
async function callCreateConsentOption(client: LoginRadius) {
  const consentOptionModel = {  }; //Required

  try {
    const res = await client.<service>.createConsentOption({ consentOptionModel });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Request body

`ConsentOptionModel` as `application/json`.

### Returns

`ConsentOptions`

---

## `deleteConsentForm`

**DELETE** `/consent/forms/{version}`

Delete Consent Form

Deletes the Consent Form identified by the form version for the Tenant.

### Example

```typescript
async function callDeleteConsentForm(client: LoginRadius) {
  const version: string = '<version>'; //Required

  try {
    const res = await client.<service>.deleteConsentForm({ version });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `version` | path | string | yes | The version of the Consent form to delete. |

### Returns

`IsDeleted`

---

## `deleteConsentOption`

**DELETE** `/consent/options/{optionId}`

Delete Consent Option

Deletes the consent option identified by the option ID for the Tenant.

### Example

```typescript
async function callDeleteConsentOption(client: LoginRadius) {
  const optionId: string = '<optionId>'; //Required

  try {
    const res = await client.<service>.deleteConsentOption({ optionId });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `optionId` | path | string | yes | The ID of the Consent option to delete. |

### Returns

`IsDeleted`

---

## `getActiveConsentForms`

**GET** `/consent/forms/active`

Retrieve Active Consent Forms

Retrieves a list of active Consent Forms configured for the Tenant.

### Example

```typescript
async function callGetActiveConsentForms(client: LoginRadius) {
  const event: string = '<event>'; //Required

  try {
    const res = await client.<service>.getActiveConsentForms({ event });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `event` | query | string | yes | Event type to filter consent verification (e.g., `login`). |

### Returns

`object`

---

## `getConsentForms`

**GET** `/consent/forms`

Retrieve Consent Forms

Retrieves all Consent Forms configured for the Tenant.

### Example

```typescript
async function callGetConsentForms(client: LoginRadius) {
  try {
    const res = await client.<service>.getConsentForms();
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Returns

`object`

---

## `getConsentOptions`

**GET** `/consent/options`

Retrieve Consent Options

Lists all consent options available for a specific Tenant.

### Example

```typescript
async function callGetConsentOptions(client: LoginRadius) {
  try {
    const res = await client.<service>.getConsentOptions();
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Returns

`object`

