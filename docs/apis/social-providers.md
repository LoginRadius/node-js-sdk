<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Social Providers

7 operation(s). Part of the [API index](../API.md).

Call these as `client.<service>.<method>({ ... })`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `deleteSocialProviderByName`

**DELETE** `/v2/manage/providers/{provider}`

Delete social provider configuration

Deletes the social provider configuration for a specified provider name for the Tenant.

### Example

```typescript
async function callDeleteSocialProviderByName(client: LoginRadius) {
  const provider: string = '<provider>'; //Required

  try {
    const res = await client.<service>.deleteSocialProviderByName({ provider });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `provider` | path | string | yes | Provider Name |

### Returns

`DeleteResponse`

---

## `getAllProviderConfigurations`

**GET** `/v2/manage/providers`

List social provider configurations

Retrieves all social provider configurations available for the Tenant.

### Example

```typescript
async function callGetAllProviderConfigurations(client: LoginRadius) {
  try {
    const res = await client.<service>.getAllProviderConfigurations();
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Returns

`object`

---

## `getEnabledProviders`

**GET** `/v2/manage/providers/active`

Retrieve enabled social providers

Retrieves a list of all enabled social providers for the Tenant.

### Example

```typescript
async function callGetEnabledProviders(client: LoginRadius) {
  try {
    const res = await client.<service>.getEnabledProviders();
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Returns

`object`

---

## `getSocialProviderByName`

**GET** `/v2/manage/providers/{provider}`

Retrieve social provider configuration

Retrieves the social provider configuration for a specified provider name for the Tenant.

### Example

```typescript
async function callGetSocialProviderByName(client: LoginRadius) {
  const provider: string = '<provider>'; //Required

  try {
    const res = await client.<service>.getSocialProviderByName({ provider });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `provider` | path | string | yes | Provider Name |

### Returns

`AppProvider`

---

## `setProvidersOrder`

**PUT** `/v2/manage/providers/setorder`

Set social provider order

Sets the order of social providers for the Tenant to be listed in the UI.

### Example

```typescript
async function callSetProvidersOrder(client: LoginRadius) {
  const object = {  }; //Required

  try {
    const res = await client.<service>.setProvidersOrder({ object });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Request body

`object` as `application/json`.

### Returns

`object`

---

## `setProvidersStatus`

**PUT** `/v2/manage/providers`

Set social provider status

Sets the status of social providers for the Tenant.

### Example

```typescript
async function callSetProvidersStatus(client: LoginRadius) {
  const providerStatusList = {  }; //Required

  try {
    const res = await client.<service>.setProvidersStatus({ providerStatusList });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Request body

`ProviderStatusList` as `application/json`.

### Returns

`object`

---

## `updateSocialProviderByName`

**PUT** `/v2/manage/providers/{provider}`

Update social provider configuration

Updates the social provider configuration for a specified provider name for the Tenant.

### Example

```typescript
async function callUpdateSocialProviderByName(client: LoginRadius) {
  const provider: string = '<provider>'; //Required
  const appProvider = {  }; //Required

  try {
    const res = await client.<service>.updateSocialProviderByName({ provider, appProvider });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `provider` | path | string | yes | Provider Name |

### Request body

`AppProvider` as `application/json`.

### Returns

`AppProvider`

