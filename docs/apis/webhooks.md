<!-- Generated by the LoginRadius SDK generator; DO NOT EDIT. -->

# Webhooks

6 operation(s). Part of the [API index](../API.md).

Call these as `client.<service>.<method>({ ... })`. `<service>` is the client field for this
group — the [README](../../README.md) lists the field name for every service.
These pages group operations the way the OpenAPI specification tags them, which
is not always one field per page.

---

## `createWebhookConfiguration`

**POST** `/v2/manage/webhooks`

Create webhook configuration

Creates a new webhook configuration for the Tenant, allowing registration of a webhook with details such as the Target URL and subscribed events.

### Example

```typescript
async function callCreateWebhookConfiguration(client: LoginRadius) {
  const webhookSubscriptionCreateModel = {  }; //Required

  try {
    const res = await client.<service>.createWebhookConfiguration({ webhookSubscriptionCreateModel });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Request body

`WebhookSubscriptionCreateModel` as `application/json`.

### Returns

`WebhookSubscription`

---

## `deleteWebhookConfigurationById`

**DELETE** `/v2/manage/webhooks/{hookId}`

Delete webhook configuration

Deletes a specific webhook configuration for the Tenant using its unique ID, permanently removing the webhook from receiving further event notifications.

### Example

```typescript
async function callDeleteWebhookConfigurationById(client: LoginRadius) {
  const hookId: string = '<hookId>'; //Required

  try {
    const res = await client.<service>.deleteWebhookConfigurationById({ hookId });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Returns

`DeleteResponse`

---

## `getAllEvents`

**GET** `/v2/manage/webhooks/events`

List webhook events

Retrieves a list of all available webhook events that can be subscribed to by the Tenant for configuring webhooks to receive notifications for specific activities.

### Example

```typescript
async function callGetAllEvents(client: LoginRadius) {
  try {
    const res = await client.<service>.getAllEvents();
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Returns

`WebhookEvents`

---

## `getAllWebhooksConfigurations`

**GET** `/v2/manage/webhooks`

List webhook configurations

Retrieves a list of all configured webhooks for the Tenant, including detailed information about each webhook and its subscribed events.

### Example

```typescript
async function callGetAllWebhooksConfigurations(client: LoginRadius) {
  try {
    const res = await client.<service>.getAllWebhooksConfigurations();
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Returns

`WebhookSubscriptionResponse`

---

## `getWebhookConfigurationById`

**GET** `/v2/manage/webhooks/{hookId}`

Retrieve webhook configuration

Retrieves the details of a specific webhook configuration for the Tenant by its unique ID, including the Target URL, subscribed events, and other settings.

### Example

```typescript
async function callGetWebhookConfigurationById(client: LoginRadius) {
  const hookId: string = '<hookId>'; //Required

  try {
    const res = await client.<service>.getWebhookConfigurationById({ hookId });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Returns

`WebhookSubscription`

---

## `updateWebhookConfigurationById`

**PUT** `/v2/manage/webhooks/{hookId}`

Update webhook configuration

Updates an existing webhook configuration for the Tenant by its unique ID, modifying details such as the Target URL, subscribed events, or other settings.

### Example

```typescript
async function callUpdateWebhookConfigurationById(client: LoginRadius) {
  const hookId: string = '<hookId>'; //Required
  const webhookSubscriptionUpdateModel = {  }; //Required

  try {
    const res = await client.<service>.updateWebhookConfigurationById({ hookId, webhookSubscriptionUpdateModel });
    console.log(res.data);
  } catch (err) {
    if (err instanceof LoginRadiusError) {
      console.error(err.description);
    } else {
      throw err;
    }
  }
}
```

### Parameters

| Name | In | Type | Required | Description |
| --- | --- | --- | --- | --- |
| `hookId` | path | string | yes | Webhook ID |

### Request body

`WebhookSubscriptionUpdateModel` as `application/json`.

### Returns

`WebhookSubscription`

